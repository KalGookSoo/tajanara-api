<configuration debug="true">

    <!-- 공통 속성(property) 정의 -->
    <property name="LOG_PATH" value="./logs" /> <!-- 로그 파일 경로 -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="application"/>
    <property name="LOG_FILE" value="${APP_NAME}.log" /> <!-- 로그 파일 이름 -->
    <property name="MAX_HISTORY" value="14" /> <!-- 보관할 최대 로그 파일 개수 -->
    <property name="TOTAL_SIZE_CAP" value="1GB" /> <!-- 보관 가능한 전체 로그 파일 크기 -->
    <property name="MAX_FILE_SIZE" value="10MB" /> <!-- 로그 파일의 최대 크기 -->

    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wex</pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender (파일 로깅 및 롤링 설정) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}</file> <!-- 로그 파일 경로/이름 -->

        <!-- 로그 롤링 정책 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 롤링될 파일의 이름 패턴, 하루 단위 로그 롤링 및 압축 -->
            <fileNamePattern>${LOG_PATH}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>

            <!-- 파일 최대 크기 -->
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <!-- 보관할 최대 일수 -->
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <!-- 로그 전체 크기 제한 -->
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- prod 프로파일일 때는 console과 file 모두 사용 -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
        </root>
    </springProfile>

    <!-- prod가 아닌 다른 프로파일일 때는 console만 사용 -->
    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>


</configuration>
